name: Auto Optimize Images
on:
  push:
    branches:
      - main
    paths:
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'

permissions:
  contents: write

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Install ImageMagick
        run: sudo apt-get update && sudo apt-get install -y imagemagick

      - name: Optimize Images
        run: |
          # Якість 92%, максимум 2560px — чітко на будь-якому телефоні
          find . -type f \( -name "*.jpg" -o -name "*.jpeg" \) -exec mogrify -resize "2560x2560>" -quality 92 -strip {} +
          find . -type f -name "*.png" -exec mogrify -resize "2560x2560>" -strip {} +

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "HQ auto-optimized images (92%, 2560px)"
