name: Auto Optimize Images
on:
  push:
    branches:
      - main
    paths:
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'

permissions:
  contents: write

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Install ImageMagick
        run: sudo apt-get update && sudo apt-get install -y imagemagick

      - name: Optimize Images
        run: |
          # Безпечне стиснення: висока якість (85%), м'яке зменшення розміру (1920px)
          find . -type f \( -name "*.jpg" -o -name "*.jpeg" \) -exec mogrify -resize "1920x1920>" -quality 85 -strip {} +
          find . -type f -name "*.png" -exec mogrify -resize "1920x1920>" -strip {} +

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Soft auto-compressed and resized images (HQ)"
