name: Auto Optimize Images
on:
  push:
    branches:
      - main
    paths:
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'

permissions:
  contents: write

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Optimize Images
        run: |
          # Використовуємо вбудований інструмент для стиснення JPG та PNG
          # Якість 80%, видалення зайвих даних (метаданих)
          find . -type f \( -name "*.jpg" -o -name "*.jpeg" \) -exec mogrify -quality 80 -strip {} +
          find . -type f -name "*.png" -exec mogrify -strip {} +

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-compressed images"
